

let soustraction = function(nbr1, nbr2) {
	let result = (nbr1 - nbr2);
		 console.log(result);
}
soustraction(51, 25);

let division = function(nbr1, nbr2) {
	let result = (nbr1 / nbr2);
		 console.log(result);
}
division(44, 2);

let multiplication = function(nbr1, nbr2) {
	let result = (nbr1 * nbr2);
		 console.log(result);
}
multiplication(44, 2);

let pourcentage = function(pourcentage, valeur) { 
	let result = (valeur * pourcentage /100);
		 console.log(pourcentage + "% de " + valeur + " = " + result);
}
pourcentage(44, 2);

let vitesse = function(distance, time) {
	let result = distance / time + "km.h";
	 console.log(result);
}
vitesse(100, 50);






